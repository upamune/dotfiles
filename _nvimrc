" Basic
if filereadable(expand('~/.nvim/basic.vim'))
  source ~/.nvim/basic.vim
endif

" KeyBindings
if filereadable(expand('~/.nvim/keybindings.vim'))
  source ~/.nvim/keybindings.vim
endif

" Plugins
if filereadable(expand('~/.nvim/plugins.vim'))
  source ~/.nvim/plugins.vim
endif

" Languages
" Python
if filereadable(expand('~/.nvim/python.vim'))
  source ~/.nvim/python.vim
endif
" Golang
if filereadable(expand('~/.nvim/golang.vim'))
  source ~/.nvim/golang.vim
endif
