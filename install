#!/bin/bash -eu
# Copyright (C) 2017 Yu SERIZAWA(@upamune)

# Deploy files
cd $(dirname $BASH_SOURCE)
BASE=$(pwd)
for rc in *rc *profile tmux.conf.local; do
  mkdir -pv bak
  [ -e ~/.$rc ] && mv -v ~/.$rc bak/.$rc
  ln -sfv $BASE/$rc ~/.$rc
done

